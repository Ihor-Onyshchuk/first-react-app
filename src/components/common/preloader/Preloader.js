import React from "react";
import preloader from "../../../assets/img/loader2.svg";

let Preloader = props => {
  return (
    <div>
      <img src={preloader} />
    </div>
  );
};

export default Preloader;
